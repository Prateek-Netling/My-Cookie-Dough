<div class="gallery1">
 {% assign i = 1 %}
 {% for block in section.blocks %}
  {% case block.type %}
   {% when 'image' %}
<!--     <img class="animation {% if i==1 %} active {% assign i = 0 %} {% endif %}" src="{{ block.settings.offer-image | image_url }}" alt="{{ block.settings.offer-image.alt }}"> -->
      {% if i==1 %}
        <img class="animation active" src="{{ block.settings.offer-image | image_url }}" alt="{{ block.settings.offer-image.alt }}">
<!--         {% assign i = 0 %} -->
<!--       {% else %}
        <img class="animation" src="{{ block.settings.offer-image | image_url }}" alt="{{ block.settings.offer-image.alt }}"> -->
      {% endif %}
  {% endcase %}
 {% endfor %}
</div>

{% schema %}
  {
    "name": "What We Offer Image",
    "settings": [],
	"class": "col-xl-4 col-md-6 pe-lg-4 column mb-md-0",
	"blocks": [
		{
			"name": "Image",
			"type": "image",
            "settings": [
              {
                "id": "offer-image",
                "type": "image_picker",
                "label": "Choose Image"
              }
            ]
		}
	],
	"presets": [
		{
			"name": "What We Offer"
		}
	]
  }
{% endschema %}